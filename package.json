{
  "name": "engagement-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Google Docs side panel extension for local engagement and robotic-writing analysis.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
